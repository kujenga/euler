#include <stdio.h>
#include<stdlib.h>

#define size 20

int main ( int argc, char ** argv )
{
  //  int array[size][size];
  int* tst = malloc (400*sizeof(int));
  int n;
  int p = 0;
  scanf("%i ",&n);
  //fill 2d array with the inputted grid
  while ( EOF != scanf( "%i", &n ) ) {
    printf("%i ",n);
    tst[p] = n;
    //array[ p%size ][ p/size ] = n;
    p++;
  }
  puts("\n");

  for ( int i=0; i < 400; i++ ) {
    if ( tst[i] == 0 ) printf( "00 " );
    else if ( tst[i] < 10 ) printf( "0%i ",tst[i] );
    else printf( "%i ",tst[i] );
    if ((i+1)%20 == 0) puts("");
  }
  /*
  //print out grid
  for ( int x = 0; x < size; x++ ) {
    for ( int y = 0; y < size; y++ ) {
      if ( tst[10*x+y] == 0 ) printf( "00 " );
      else if ( tst[10*x+y] < 10 ) printf( "0%i ",tst[10*x+y] );
      else printf( "%i ",tst[10*x+y] );
    }
    printf("\n");
  }
  */
}
